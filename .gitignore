# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/

# Secrets / local env overrides (keep docker/.env.example committed)
.env
.env.local
.env.*.local
!.env.example
!docker/.env.example

# docker/.env may contain local OLLAMA_URL overrides (remote tunnel).
# Commit pins via docker/.env.example; copy to docker/.env locally.
docker/.env

# Node / Next.js
node_modules/
frontend/.next/
frontend/out/
npm-debug.log*
yarn-error.log*

# Data artifacts
data/*.json
backend/data/sample_logs.json
sample_logs.json

# OS / IDE
.DS_Store
.idea/
.vscode/
*.swp
.agent/

# Local-only notes (project docs under docs/ and root *.md ARE tracked)
*.local.md
notes.local.md
